const year 0x2023
var month 0x11
var day 0x22

Main:
proc
	loop 3
		-- currently values are only printed in hex format
		log message 0 "Hello World {}-{}-{}" $year $month $day
		wait 1000
	end loop
	finish
end proc
